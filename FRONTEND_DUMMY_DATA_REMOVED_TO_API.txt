================================================================================
Dummy / mock data removed — replacement API mapping
================================================================================
Use this when debugging: each UI file should call the listed API helpers; HTTP
envelope is ApiResponse<T> (axios response.body = { success, data, message, … }).
List payloads are validated as JSON arrays via unwrapAxiosApiList (response.data.data).

--------------------------------------------------------------------------------
admin/Dashboard.tsx
--------------------------------------------------------------------------------
Removed: mockSites, mockUsers, mockServices (from ../../data/mockData).
Now: fetchSites, fetchRooms, fetchServices, fetchUsers + mapSiteRows (same file
patterns as admin/Sites).

APIs:
  GET /api/masterdata/sites?page=&pageSize=
  GET /api/masterdata/rooms?page=&pageSize=
  GET /api/masterdata/services
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
admin/Sites.tsx
--------------------------------------------------------------------------------
Removed: mockSites.
Now: fetchSites + fetchRooms + mapSiteRows.

APIs:
  GET /api/masterdata/sites
  GET /api/masterdata/rooms

--------------------------------------------------------------------------------
admin/Services.tsx
--------------------------------------------------------------------------------
Removed: mockServices.
Now: fetchServices + mapServiceRow.

APIs:
  GET /api/masterdata/services

--------------------------------------------------------------------------------
admin/Users.tsx
--------------------------------------------------------------------------------
Removed: mockUsers.
Now: fetchUsers + fetchProviders + buildProviderSpecialtyMap + mapUserRows.

APIs:
  GET /api/iam/users
  GET /api/masterdata/providers

--------------------------------------------------------------------------------
admin/Providers.tsx
--------------------------------------------------------------------------------
Removed: mockUsers (provider subset).
Now: fetchProviders + per-row fetchServicesByProvider + mapProviderRows.

APIs:
  GET /api/masterdata/providers
  GET /api/masterdata/providers/{providerId}/services

--------------------------------------------------------------------------------
frontdesk/Booking.tsx
--------------------------------------------------------------------------------
Removed: mockUsers (providers), mockServices (mockSites was imported unused).
Now: fetchProviders + fetchServices; provider/service cards from API.

APIs:
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
pages/AdminDashboard.tsx (legacy full-screen admin with Header + tabs)
--------------------------------------------------------------------------------
Removed: mockSites, mockUsers, mockServices imports.
UI preserved: same layout (stat cards, charts, tabbed tables, modal) as before.
Data now: fetchSites + fetchRooms + mapSiteRows; fetchUsers + fetchProviders +
buildProviderSpecialtyMap + mapUserRows; fetchServices + mapServiceRow.
(Note: App router still uses pages/admin/Dashboard.tsx for /admin; this file is
for any separate entry that still imports pages/AdminDashboard.)

APIs:
  GET /api/masterdata/sites
  GET /api/masterdata/rooms
  GET /api/iam/users
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
mockData.ts
--------------------------------------------------------------------------------
Removed exports: mockUsers, mockServices (no remaining imports).
Still exported for screens not yet on API: mockAppointments, mockSites (for
operations/Roster only), mockShifts, mockLeaveRequests, mockWaitlist,
utilizationData, appointmentStatusData, providerUtilization.

--------------------------------------------------------------------------------
provider/Appointments.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: meApi + fetchUsers + searchAppointments(providerId) + masterdata maps.

APIs:
  GET /auth/me
  GET /api/iam/users?page=&pageSize=
  GET /appointments?providerId=
  GET /api/masterdata/services
  GET /api/masterdata/sites

--------------------------------------------------------------------------------
provider/Dashboard.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: meApi + fetchUsers + searchAppointments(providerId) + masterdata maps.

APIs:
  GET /auth/me
  GET /api/iam/users?page=&pageSize=
  GET /appointments?providerId=
  GET /api/masterdata/services
  GET /api/masterdata/sites

--------------------------------------------------------------------------------
patient/Appointments.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: meApi + searchAppointments(patientId) + provider/service/site maps.
Cancel action now calls backend cancel API and refreshes list.

APIs:
  GET /auth/me
  GET /appointments?patientId=
  PATCH /appointments/{id}/cancel
  GET /api/masterdata/providers
  GET /api/masterdata/services
  GET /api/masterdata/sites

--------------------------------------------------------------------------------
patient/Dashboard.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: meApi + searchAppointments(patientId) + provider/service/site maps.

APIs:
  GET /auth/me
  GET /appointments?patientId=
  GET /api/masterdata/providers
  GET /api/masterdata/services
  GET /api/masterdata/sites

--------------------------------------------------------------------------------
frontdesk/Dashboard.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments, mockWaitlist.
Now: searchAppointments(date=today) + searchWaitlist + user/provider/service maps.

APIs:
  GET /appointments?date=
  GET /waitlist
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
frontdesk/Queue.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: searchAppointments(date=today) + user/provider/service maps.
Check-in action now calls backend checked-in API and refreshes queue.

APIs:
  GET /appointments?date=
  PATCH /appointments/{id}/checked-in
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
frontdesk/CheckIn.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: searchAppointments(date=today,status=Booked) + user/provider/service maps.
Check-in action now calls backend checked-in API and refreshes cards.

APIs:
  GET /appointments?date=&status=Booked
  PATCH /appointments/{id}/checked-in
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
frontdesk/Waitlist.tsx
--------------------------------------------------------------------------------
Removed: mockWaitlist.
Now: searchWaitlist + patient/provider/service maps.

APIs:
  GET /waitlist
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
operations/Dashboard.tsx
--------------------------------------------------------------------------------
Removed: mockShifts, mockLeaveRequests.
Now: searchRosters + searchLeaveRequests + searchOnCall + user mapping.
Leave action buttons now call backend approve/reject and refresh.

APIs:
  GET /rosters
  GET /leave
  GET /oncall
  PATCH /leave/{leaveId}/approve
  PATCH /leave/{leaveId}/reject
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
operations/Roster.tsx
--------------------------------------------------------------------------------
Removed: mockShifts, mockSites.
Now: searchOnCall + fetchSites + user mapping for weekly roster grid.

APIs:
  GET /oncall
  GET /api/masterdata/sites?page=&pageSize=
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
operations/Leave.tsx
--------------------------------------------------------------------------------
Removed: mockLeaveRequests.
Now: searchLeaveRequests + user mapping.
Approve/reject actions now call backend and refresh list.

APIs:
  GET /leave
  PATCH /leave/{leaveId}/approve
  PATCH /leave/{leaveId}/reject
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
operations/OnCall.tsx
--------------------------------------------------------------------------------
Removed: local onCallSchedule array.
Now: searchOnCall + user mapping for current and full schedule tables.

APIs:
  GET /oncall
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
staff/Schedule.tsx
--------------------------------------------------------------------------------
Removed: mockShifts.
Now: meApi + searchRosterAssignments + searchOnCall + fetchSites + user mapping.

APIs:
  GET /auth/me
  GET /roster-assignments
  GET /oncall
  GET /api/masterdata/sites?page=&pageSize=
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
staff/Dashboard.tsx
--------------------------------------------------------------------------------
Removed: mockShifts, mockAppointments.
Now: meApi + searchRosterAssignments + searchOnCall + searchAppointments(date=today)
+ user/provider/service mapping for schedule and cards.

APIs:
  GET /auth/me
  GET /roster-assignments
  GET /oncall?date=
  GET /appointments?date=
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
staff/Patients.tsx
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: searchAppointments(date=today) + patient/provider/service/site mappings.

APIs:
  GET /appointments?date=
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services
  GET /api/masterdata/sites?page=&pageSize=

--------------------------------------------------------------------------------
admin/Reports.tsx
--------------------------------------------------------------------------------
Removed: utilizationData, appointmentStatusData, providerUtilization.
Now: report metrics are loaded from backend reports and mapped into chart arrays.

APIs:
  GET /reports?scope=Operations

--------------------------------------------------------------------------------
pages/FrontDeskDashboard.tsx (legacy full-screen)
--------------------------------------------------------------------------------
Removed: mockAppointments, mockWaitlist.
Now: searchAppointments(date=today) + searchWaitlist + user/provider/service maps.
Check-in action calls backend checked-in API and refreshes list.

APIs:
  GET /appointments?date=
  PATCH /appointments/{id}/checked-in
  GET /waitlist
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
pages/ProviderDashboard.tsx (legacy full-screen)
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: meApi + searchAppointments(providerId) + user/site/service mapping.

APIs:
  GET /auth/me
  GET /appointments?providerId=
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services
  GET /api/masterdata/sites?page=&pageSize=

--------------------------------------------------------------------------------
pages/PatientDashboard.tsx (legacy full-screen)
--------------------------------------------------------------------------------
Removed: mockAppointments.
Now: meApi + searchAppointments(patientId) + provider/service/site mapping.

APIs:
  GET /auth/me
  GET /appointments?patientId=
  GET /api/masterdata/providers
  GET /api/masterdata/services
  GET /api/masterdata/sites?page=&pageSize=

--------------------------------------------------------------------------------
pages/OperationsDashboard.tsx (legacy full-screen)
--------------------------------------------------------------------------------
Removed: mockShifts, mockLeaveRequests, utilizationData, providerUtilization.
Now: searchOnCall + searchLeaveRequests + searchRosters + users mapping.

APIs:
  GET /oncall
  GET /leave
  GET /rosters
  GET /api/iam/users?page=&pageSize=

--------------------------------------------------------------------------------
pages/StaffDashboard.tsx (legacy full-screen)
--------------------------------------------------------------------------------
Removed: mockShifts, mockAppointments.
Now: meApi + searchRosterAssignments + searchOnCall + searchAppointments(date=today)
+ users/provider/service mapping.

APIs:
  GET /auth/me
  GET /roster-assignments
  GET /oncall?date=
  GET /appointments?date=
  GET /api/iam/users?page=&pageSize=
  GET /api/masterdata/providers
  GET /api/masterdata/services

--------------------------------------------------------------------------------
mockData.ts (final cleanup)
--------------------------------------------------------------------------------
Removed file: src/app/data/mockData.ts (no remaining imports in src/app/pages).
All active and legacy pages now consume backend APIs through src/api modules.

================================================================================
End of mapping (append new rows when more pages drop mocks)
================================================================================
